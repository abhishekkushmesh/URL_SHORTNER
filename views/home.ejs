<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Home - URL Shortener</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <a class="navbar-brand" href="/">URL Shortener</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav ms-auto">
  <li class="nav-item"><a class="nav-link" href="/">Home</a></li>

  <% if (user) { %>
    <% 
       const displayName = user.name || user.email.split('@')[0];
       const initial = displayName.charAt(0).toUpperCase(); 
    %>

    <li class="nav-item d-flex align-items-center ms-2">
      <div class="rounded-circle bg-light text-primary d-flex justify-content-center align-items-center fw-bold shadow-sm" 
           style="width: 35px; height: 35px; font-size: 1.1rem; border: 2px solid #fff;">
        <%= initial %>
      </div>
    </li>

    <li class="nav-item d-flex align-items-center">
      <span class="nav-link text-white fw-semibold px-2">
        <%= displayName %>
      </span>
    </li>

    <li class="nav-item d-flex align-items-center ms-2">
      <a class="btn btn-sm btn-danger rounded-pill px-3" href="/user/logout">Logout</a>
    </li>

  <% } else { %>
    
    <li class="nav-item"><a class="nav-link" href="/signup">Signup</a></li>
    <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>

  <% } %>
</ul>
        </div>
      </div>
    </nav>

    <main class="container py-5">
      <div class="row">
        <div class="col-lg-6">
          <div class="card shadow-sm mb-4">
            <div class="card-body">
              <h5 class="card-title">Create a Short URL</h5>
              <form method="POST" action="/url" class="row g-2">
                <div class="col-12">
                  <label class="form-label">Original URL</label>
                  <input class="form-control" type="url" name="url" placeholder="https://example.com" required>
                </div>
                <div class="col-auto">
                  <button class="btn btn-primary" type="submit">Generate</button>
                </div>
              </form>

              <% if (locals.id) { %>
                <div class="alert alert-success mt-3">
                  <strong>Short URL:</strong>
                  <a href="/url/<%= id %>" target="_blank">http://localhost:8001/url/<%= id %></a>
                </div>
              <% } %>
            </div>
          </div>

          <div class="card shadow-sm">
            <div class="card-body">
              <h5 class="card-title">Your Links</h5>
              <% if (!locals.urls || locals.urls.length === 0) { %>
                <p class="text-muted">No links yet. Generate one above.</p>
              <% } else { %>
                <div class="table-responsive">
                  <table class="table table-sm align-middle">
                    <thead>
                      <tr>
                        <th>#</th>
                        <th>Short ID</th>
                        <th>Original URL</th>
                        <th>Clicks</th>
                        <th>Action</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% urls.forEach((url, index) => { %>
                        <tr>
                          <td><%= index + 1 %></td>
                          <td><code><%= url.shortId %></code></td>
                          <td class="text-truncate" style="max-width:300px;"><a href="<%= url.redirectURL %>" target="_blank"><%= url.redirectURL %></a></td>
                          <td><%= (url.visitHistory && url.visitHistory.length) || 0 %></td>
                          <td>
                            <a class="btn btn-sm btn-outline-primary" href="/url/<%= url.shortId %>" target="_blank">Open</a>
                            <a class="btn btn-sm btn-outline-secondary" href="/url/analytics/<%= url.shortId %>">Analytics</a>
                          </td>
                        </tr>
                      <% }) %>
                    </tbody>
                  </table>
                </div>
              <% } %>
            </div>
          </div>
        </div>

        <div class="col-lg-6">
          <div class="card shadow-sm">
            <div class="card-body">
              <h5 class="card-title">How it works</h5>
              <ol>
                <li>Sign up and log in.</li>
                <li>Paste a long URL and click Generate.</li>
                <li>Share the short URL — visits are tracked automatically.</li>
              </ol>
              <hr>
              <h6 class="mb-2">Quick Tips</h6>
              <ul>
                <li>Use full URLs with protocol (https://).</li>
                <li>Click "Analytics" to view visit timestamps and total clicks.</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>